class Application(val name: String) {

    // write the run method here
    fun run(one: String, two: String, three: String) {
        println(name)
        println(one)
        println(two)
        println(three)

    }
}